{
  "name": "fetch-timeout-retry",
  "version": "1.0.1",
  "description": "Extended Fetch API with timeout and retries.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "ts-node ./tests/index.ts",
    "npm-publish-patch": "tsc && npm version patch && npm publish",
    "npm-publish-minor": "tsc && npm version minor && npm publish",
    "npm-publish-major": "tsc && npm version major && npm publish"
  },
  "author": "Murilo Schünke",
  "homepage": "https://github.com/mschunke/fetch-timeout-retry",
  "repository": {
    "type": "git",
    "url": "https://github.com/mschunke/fetch-timeout-retry.git"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.5.0",
    "ts-node": "^10.8.1"
  }
}
